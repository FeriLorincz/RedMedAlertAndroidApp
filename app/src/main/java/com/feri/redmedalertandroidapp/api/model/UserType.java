package com.feri.redmedalertandroidapp.api.model;

public enum UserType {

    ELDER,           // Persoana în vârstă monitorizată
    FAMILY_MEMBER,   // Aparținător (fiu/fiică/etc)
    CAREGIVER        // Poate fi orice alta persoana de care vom avea nevoie
}
